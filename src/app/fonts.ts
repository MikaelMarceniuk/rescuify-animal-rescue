import { Jost } from 'next/font/google'
import { Mukta } from 'next/font/google'

export const jost = Jost({ subsets: ['latin'] })

export const mukta = Mukta({ subsets: ['latin'], weight: ['500', '800'] })
